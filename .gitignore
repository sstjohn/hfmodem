__pycache__/
*.pyc
.venv/
*.egg-info/
build/
.pytest_cache/
.ruff_cache/
.DS_Store

# On-air session products. shrike's --outdir defaults to captures/onair and
# ota's to captures/ota; delivered mail lands in logs/mail. Recordings are
# evidence — keep them somewhere deliberate, not in the checkout.
captures/
logs/
